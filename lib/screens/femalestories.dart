import 'package:flutter/material.dart';

class StoriesFemalePage extends StatelessWidget {
  const StoriesFemalePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('This is female stories page'),
    );
  }
}
